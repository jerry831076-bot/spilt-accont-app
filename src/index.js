import React from 'react';
import ReactDOM from 'react-dom';
import App from './App'; // 引用了您剛才編輯的 App.jsx

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);